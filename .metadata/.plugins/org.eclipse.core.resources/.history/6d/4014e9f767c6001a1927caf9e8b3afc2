package dynamicprogramming;

public class GoldMineProblem {

	public int findMaxGold(int [][]matrix, int startRow, int startCol) {
		if(startRow < 0 || startRow >= matrix.length || startCol < 0 || startCol >=matrix.length) {
			return 0;
		}
		int result = 0;
		for(int i = 0; i)
	}

}
